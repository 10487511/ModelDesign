/**名称*/
单例模式

/**定义*/
确保某个类只有一个实例，自行实例化，并提供这个实例

/**应用场景*/
·频繁访问数据库或者文件对象
·工具类对象
·创建对象时耗时过多或耗费资源过多，但又经常需要使用的对象

/**优点*/
·内存中只存在一个对象，节省了系统资源
·避免对资源的多重占用

/**缺点*/
·单例对象如果持有上下文，很容易引起内存泄露
·单例模式一般没有接口，扩展很困难，后续需要扩展需要修改代码来实现