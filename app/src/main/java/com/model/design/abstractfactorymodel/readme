/**名称*/
抽象工厂模式

/**定义*/
为创建一组相关或者相互依赖的对象提供一个借口，而不需要指定他们具体的类

/**应用场景*/
·生产多个产品组合的对象时

/**优点*/
·代码解耦，创建实例的工作与使用实例的工作分开，使用者不必关系对象如何创建

/**缺点*/
·违背封闭原则，如果需要添加新产品则必须修改工厂类和具体工厂逻辑

/**工厂方法模式与抽象工厂模式比较*/
·在工厂方法模式中具体工厂复杂生产具体的产品，每一个具体工厂对应一种具体产品，工厂方法具有唯一性
·抽象工厂模式则可以提供多个产品对象，而不是单一的产品对象

/**源码分析*/
